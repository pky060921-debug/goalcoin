//#region src/stores.d.ts
/**
 * An sync key-value store.
 */
interface SyncStore {
  get(key: string): string | null;
  set(key: string, value: string): void;
  delete(key: string): void;
}
/**
 * Create a storage interface backed by memory.
 * This is generally useful for server-side rendering, and test environments.
 */
declare function createInMemoryStorage(): SyncStore;
/**
 * Create a store backed by `localStorage`.
 */
declare function createLocalStorage(): SyncStore;
/**
 * Create a store backed by `sessionStorage`.
 */
declare function createSessionStorage(): SyncStore;
//#endregion
export { SyncStore, createInMemoryStorage, createLocalStorage, createSessionStorage };
//# sourceMappingURL=stores.d.mts.map