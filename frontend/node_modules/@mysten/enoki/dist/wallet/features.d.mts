import { AuthProvider, EnokiNetwork } from "../EnokiClient/type.mjs";
import { ZkLoginSession } from "./types.mjs";

//#region src/wallet/features.d.ts
/** Name of the feature for retrieving basic wallet metadata. */
declare const EnokiGetMetadata = "enoki:getMetadata";
/** The latest API version of the getMetadata API. */
type EnokiGetMetadataVersion = '1.0.0';
/**
 * A Wallet Standard feature for retrieving Enoki-specific metadata about the wallet.
 */
type EnokiGetMetadataFeature = {
  /** Namespace for the feature. */
  [EnokiGetMetadata]: {
    /** Version of the feature API. */
    version: EnokiGetMetadataVersion;
    getMetadata: EnokiGetMetadataMethod;
  };
};
type EnokiGetMetadataMethod = (input?: EnokiGetMetadataInput) => EnokiGetMetadataOutput;
/** Input for retrieving metadata about the wallet. */
type EnokiGetMetadataInput = void;
/** Output of retrieving metadata about the wallet. */
type EnokiGetMetadataOutput = {
  /** The social provider for the wallet. */
  provider: AuthProvider;
};
/** Name of the feature for retrieving the Enoki session. */
declare const EnokiGetSession = "enoki:getSession";
/** The latest API version of the getSession API. */
type EnokiGetSessionVersion = '1.0.0';
/**
 * A Wallet Standard feature for retrieving the zkLogin session from the wallet.
 */
type EnokiGetSessionFeature = {
  /** Namespace for the feature. */
  [EnokiGetSession]: {
    /** Version of the feature API. */
    version: EnokiGetSessionVersion;
    getSession: EnokiGetSessionMethod;
  };
};
type EnokiGetSessionMethod = (input?: EnokiGetSessionInput) => Promise<EnokiGetSessionOutput>;
/** Input for retrieving the session from the wallet. */
type EnokiGetSessionInput = {
  /**
   * The network to retrieve session data for. Defaults to the active network.
   **/
  network?: EnokiNetwork;
};
/** Output of retrieving the Enoki session. */
type EnokiGetSessionOutput = ZkLoginSession | null;
//#endregion
export { EnokiGetMetadataFeature, EnokiGetMetadataOutput, EnokiGetSessionFeature, EnokiGetSessionInput, EnokiGetSessionOutput };
//# sourceMappingURL=features.d.mts.map