import { registerEnokiWallets } from "./register.mjs";

//#region src/wallet/initializer.ts
function enokiWalletsInitializer(options) {
	return {
		id: "enoki-wallets-initializer",
		async initialize({ networks, getClient }) {
			const { unregister } = registerEnokiWallets({
				...options,
				getCurrentNetwork: () => getClient().network,
				clients: networks.map(getClient)
			});
			return { unregister };
		}
	};
}

//#endregion
export { enokiWalletsInitializer };
//# sourceMappingURL=initializer.mjs.map