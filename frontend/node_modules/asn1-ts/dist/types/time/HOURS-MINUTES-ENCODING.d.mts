import type { INTEGER } from "../../macros.mjs";
export default class HOURS_MINUTES_ENCODING {
    readonly hours: INTEGER;
    readonly minutes: INTEGER;
    constructor(hours: INTEGER, minutes: INTEGER);
}
