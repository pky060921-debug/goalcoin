import convertBytesToText from "../../../utils/convertBytesToText.mjs";
import validateTime from "../../../validators/validateTime.mjs";
export default function decodeTimeOfDay(bytes) {
    const str = convertBytesToText(bytes);
    const hours = parseInt(str.slice(0, 2), 10);
    const minutes = parseInt(str.slice(2, 4), 10);
    const seconds = parseInt(str.slice(4, 6), 10);
    validateTime("TIME-OF-DAY", hours, minutes, seconds);
    const ret = new Date();
    ret.setHours(hours);
    ret.setMinutes(minutes);
    ret.setSeconds(seconds);
    return ret;
}
