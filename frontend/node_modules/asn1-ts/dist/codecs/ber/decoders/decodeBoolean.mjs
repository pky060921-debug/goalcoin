import * as errors from "../../../errors.mjs";
export default function decodeBoolean(value) {
    if (value.length !== 1) {
        throw new errors.ASN1SizeError("BOOLEAN not one byte");
    }
    return (value[0] !== 0);
}
